# 配置（含密钥）
config/config.yaml
config/config.local.yaml
secrets/

# 运行时数据（记忆 / 图库 / 日志）
data/
*.state.json

# 审查用的合并文档（含完整源码副本，不随仓库分发）
docs/review-bundle.md

# 只在本地有效的敏感词表（仓库里只留 .example 模板）
scripts/private-names.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 编辑器
.vscode/
.idea/
.DS_Store
